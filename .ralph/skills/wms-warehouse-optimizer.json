{
  "name": "wms-warehouse-optimizer",
  "description": "Analyze and optimize warehouse operations: pick paths, bin locations, inventory",
  "parameters": {
    "type": "object",
    "properties": {
      "locations": {
        "type": "array",
        "description": "Warehouse bin locations to optimize",
        "items": {"type": "string"}
      },
      "sku": {
        "type": "string",
        "description": "SKU to analyze for bin placement"
      },
      "pickFrequency": {
        "type": "number",
        "description": "Pick frequency per day for the SKU"
      }
    }
  },
  "steps": [
    {
      "name": "calculate_optimal_pick_path",
      "tool": "mcp://wms-dev-accelerator/wms_calculate_pick_path",
      "arguments": {
        "locations": "{{locations}}",
        "startPoint": "A-01-01"
      }
    },
    {
      "name": "optimize_bin_location",
      "tool": "mcp://wms-dev-accelerator/wms_optimize_bin_locations",
      "arguments": {
        "sku": "{{sku}}",
        "pickFrequency": "{{pickFrequency}}"
      }
    },
    {
      "name": "analyze_picking_performance",
      "tool": "mcp://wms-dev-accelerator/wms_analyze_picking_performance",
      "arguments": {
        "pickerId": "system",
        "startDate": "{{startDate}}",
        "endDate": "{{endDate}}"
      }
    }
  ]
}
